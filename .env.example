# Production Environment Configuration
GCP_PROJECT_ID=[your_project_id]
GCP_STORAGE_BUCKET=[your_bucket_name]
GCP_SA_EMAIL=[your_service_email]
GCP_SA_KEY_BASE64=[your_key]

# The name of your application.
APP_NAME=NCAToolkit

# URL of the Endpoint
CLOUD_BASE_URL=

# Local testing base URL
LOCAL_BASE_URL=http://localhost:8080

# Debug mode setting. Set to `false` for production environments.
APP_DEBUG=false

# Your app's domain or subdomain, without the 'http://' or 'https://' prefix.
APP_DOMAIN=example.com

# Full application URL is automatically configured; no modification required.
APP_URL=https://${APP_DOMAIN}

# SSL settings
SSL_EMAIL=user@example.com

# API_KEY
# Purpose: Used for API authentication.
# Requirement: Mandatory.
API_KEY=your_api_key_here

# Security Configuration
# ----------------------

# RATE_LIMIT_PER_MINUTE
# Purpose: Maximum number of requests allowed per minute per IP/API key.
# Default: 100
# Requirement: Optional.
RATE_LIMIT_PER_MINUTE=100

# RATE_LIMIT_BURST
# Purpose: Maximum burst capacity for rate limiting.
# Default: Same as RATE_LIMIT_PER_MINUTE or 100 (whichever is higher)
# Requirement: Optional.
RATE_LIMIT_BURST=100

# RATE_LIMIT_KEY
# Purpose: Key used for rate limiting ("ip" or "api_key").
# Default: ip
# Requirement: Optional.
RATE_LIMIT_KEY=ip

# ENABLE_SECURITY_HEADERS
# Purpose: Enable security headers (X-Content-Type-Options, X-Frame-Options, etc.).
# Default: true
# Requirement: Optional.
ENABLE_SECURITY_HEADERS=true

# ALLOWED_ORIGINS
# Purpose: Comma-separated list of allowed CORS origins. Empty means no CORS headers.
# Example: https://example.com,https://app.example.com
# Default: (empty)
# Requirement: Optional.
ALLOWED_ORIGINS=

# Feature Flags / ASR (Automatic Speech Recognition)
# ---------------------------------------------------

# ENABLE_OPENAI_WHISPER
# Purpose: Use legacy OpenAI Whisper model instead of default Faster-Whisper.
# Default: false (Faster-Whisper is now the default for better performance)
# Requirement: Optional.
ENABLE_OPENAI_WHISPER=false

# SKIP_MODEL_WARMUP
# Purpose: Skip model warm-up at container start (useful for CI/CD).
# Note: When true, model loads on first request instead of at startup.
# Default: false
# Requirement: Optional.
SKIP_MODEL_WARMUP=false

# ASR_MODEL_ID
# Purpose: Model identifier for the ASR system.
# Default: openai/whisper-base
# Options: openai/whisper-tiny, openai/whisper-base, openai/whisper-small, openai/whisper-medium, openai/whisper-large-v3
# Requirement: Optional.
ASR_MODEL_ID=openai/whisper-base

# ASR_DEVICE
# Purpose: Device to run ASR model on.
# Default: cpu
# Options: cpu, cuda, auto
# Requirement: Optional.
ASR_DEVICE=cpu

# ASR_COMPUTE_TYPE
# Purpose: Compute type for ASR model inference.
# Default: int8
# Options: int8 (faster), float16 (balanced), float32 (accurate)
# Requirement: Optional.
ASR_COMPUTE_TYPE=int8

# ASR_BEAM_SIZE
# Purpose: Beam search width for ASR decoding.
# Default: 5
# Note: Higher values may improve accuracy but increase computation time.
# Requirement: Optional.
ASR_BEAM_SIZE=5

# ASR_BATCH_SIZE
# Purpose: Batch size for ASR processing.
# Default: 16
# Note: Adjust based on available memory.
# Requirement: Optional.
ASR_BATCH_SIZE=16

# ASR_CACHE_DIR
# Purpose: Directory for caching ASR models and temporary files.
# Default: {LOCAL_STORAGE_PATH}/asr_cache
# Requirement: Optional.
ASR_CACHE_DIR=

# s3 Compatible Storage Env Vars
#
#S3_ACCESS_KEY=your_access_key
#S3_SECRET_KEY=your_secret_key
#S3_ENDPOINT_URL=https://your-endpoint-url
#S3_REGION=your-region
#S3_BUCKET_NAME=your-bucket-name


# Google Cloud Storage Env Variables
#
# GCP_SA_CREDENTIALS
# Purpose: The JSON credentials for the GCP Service Account.
# Requirement: Mandatory if using GCP storage.
#GCP_SA_CREDENTIALS=/path/to/your/gcp/service_account.json

# GCP_BUCKET_NAME
# Purpose: The name of the GCP storage bucket.
# Requirement: Mandatory if using GCP storage.
#GCP_BUCKET_NAME=your_gcp_bucket_name

# STORAGE_PATH
# Purpose: The base path for storage operations.
# Default: GCP
# Requirement: Optional.
#STORAGE_PATH=GCP

# Docker Build Configuration
# --------------------------

# BUILD_VARIANT
# Purpose: Docker build variant selection.
# Options: gpu (CUDA support), cpu (lightweight, no CUDA)
# Default: gpu
# Requirement: Build-time only.
#BUILD_VARIANT=gpu

# CUDA_VERSION
# Purpose: CUDA version for GPU builds.
# Default: 12.1.0
# Requirement: Build-time only, GPU variant.
#CUDA_VERSION=12.1.0

# CUDNN_VERSION
# Purpose: cuDNN version for GPU builds.
# Default: 8
# Requirement: Build-time only, GPU variant.
#CUDNN_VERSION=8

# Runtime Configuration
# ---------------------

# GUNICORN_WORKERS
# Purpose: Number of Gunicorn worker processes.
# Default: 2
# Note: Adjust based on available CPU cores.
# Requirement: Optional.
GUNICORN_WORKERS=2

# GUNICORN_TIMEOUT
# Purpose: Request timeout in seconds.
# Default: 300
# Note: Increase for long-running transcription tasks.
# Requirement: Optional.
GUNICORN_TIMEOUT=300
